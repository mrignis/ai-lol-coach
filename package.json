{
  "name": "lol-coach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI LoL Coach — personal weakness analysis from the official Riot API (MVP v0.1)",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "probe": "node scripts/probe.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
